fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
numpy>=1.26
networkx>=3.2
reportlab>=4.1
jinja2>=3.1
# Needed only when a hosted LLM provider or Bhashini is selected, but it must
# be present *before* that switch is flipped: both import httpx lazily, so a
# missing wheel surfaces as a ModuleNotFoundError on the first live call
# rather than at startup. Everything else here is used by the default config.
httpx>=0.27
